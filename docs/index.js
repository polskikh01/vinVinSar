const body=document.body,darkMode=document.getElementById("darkMode"),allIcon=Array.from(document.querySelectorAll("img")).slice(0,7),lm=()=>{body.classList.remove("darkMode"),allIcon.forEach(r=>r.setAttribute("src",r.currentSrc.replace("dark","light")))},dm=()=>{body.classList.add("darkMode"),allIcon.forEach(r=>r.setAttribute("src",r.currentSrc.replace("light","dark")))};darkMode.addEventListener("click",()=>{""===body.className?(body.classList.add("darkMode"),allIcon.forEach(r=>r.setAttribute("src",r.currentSrc.replace("light","dark")))):(body.classList.remove("darkMode"),allIcon.forEach(r=>r.setAttribute("src",r.currentSrc.replace("dark","light"))))});